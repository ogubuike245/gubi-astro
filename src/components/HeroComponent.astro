---
const { image } = Astro.props;
---

<article class="hero-container">
    <img src={image} alt="image one" />
    <div>
        <h1>Welcome to GUBI</h1>
        <p>Your Ultimate Source for Lifestyle and Sports News</p>
    </div>
</article>

<style>
    article {
        display: flex;
        flex-direction: column;
        background-color: var(--dark);
        box-shadow: 4px 3px 5px rgba(34, 34, 34, 0.2);
    }
    img {
        transform: rotate(180deg);
        z-index: 1;
    }

    div {
        height: 40%;
        background-color: var(--primary);
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
    }
    h1 {
        color: var(--dark);
        font-size: 2rem;
        font-weight: 900;
    }
    p {
        color: var(--dark);
        font-size: 1rem;
    }

    /* media queries  */
    @media screen and (min-width: 768px) {
        article {
            height: 80vh;
        }
    }
</style>
